# Resilience Skill — Kaizen Log

> Archived from inline Kaizen section in SKILL.md (2026-06-14).
> Active skill body lives in `SKILL.md`. Add new entries here; promote durable rules into SKILL.md body.

<!-- Kaizen: 2026-06-10 - Services table re-verified from ls: patch→patch_adapter/, sms/→sms_consent_phone_change_invalidator.rb + graphql/features/sms/, searchable.rb→*_searchable.rb concerns. -->
<!-- Kaizen: 2026-06-10 - Baselines measured and stated for "Expected: 0 matches" checks: HTTParty=17, Faraday=21, bare-rescue=19, rescue Exception=0, silent-rescue pipeline=~1060 lines (2026-06-10). -->
<!-- Kaizen: 2026-06-15 - CORRECTNESS FIX (RETRACTED - FALSE PREMISE): The 2026-06-15 entry above claimed `sidekiq_options` raises NoMethodError on bare `< ApplicationJob` subclasses and therefore the GOOD example must use `include Sidekiq::Worker`. THIS WAS WRONG. Validated facts: Sidekiq 7.3.9 ships a Rails Engine initializer (`sidekiq/rails.rb`) that runs `include ::Sidekiq::Job::Options unless respond_to?(:sidekiq_options)` on `ActiveJob::Base` at boot — so ALL ApplicationJob subclasses in this repo have `sidekiq_options` without any extra include. PROOF in repo: `app/jobs/sync_match_job.rb` is `< ApplicationJob` + `sidekiq_options lock: :until_executed` and runs in production. `app/jobs/membership_reminder_job.rb` is the ApplicationJob + `include Sidekiq::Throttled::Worker` + `sidekiq_options retry: 3` combination example. `app/jobs/merchandise_import_job.rb` is a PURE Sidekiq::Worker (no ApplicationJob) — it is NOT an example of the ApplicationJob+include pattern. Pattern 4 in SKILL.md has been restored to `< ApplicationJob` + `sidekiq_options retry: 3` as the GOOD example, with both pure-Sidekiq and combination patterns documented as alternatives. (2026-06-15, corrected by adversarial-review validator) -->
<!-- Kaizen: 2026-06-15 - LESSON: Before claiming a method is unavailable (NoMethodError), grep the repo for real callers. `grep -rn "sidekiq_options" app/jobs/ --include="*.rb"` would have shown multiple `< ApplicationJob` jobs using it. False NoMethodError claims lead to correct-looking but semantically wrong guidance. Always trace the gem's Rails Engine initializer when framework integration is in play. -->
